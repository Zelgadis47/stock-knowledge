# 永久授权：嘉娜可以直接修改 our-knowledge/ 仓库中的文件

---

## 编码安全 🔐

- 所有新文件必须使用 UTF-8 编码（无 BOM）
- Python 脚本中打开文件必须指定 `encoding='utf-8'`
- PowerShell 输出重定向必须使用 `Out-File -Encoding utf8`
- deploy-check.py 会在每次 commit 前检查全部 HTML/JS/CSS 文件的 UTF-8 合法性
- `.gitattributes` 确保 Git 在 add 阶段就拦截非 UTF-8 文件
